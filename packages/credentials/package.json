{
  "name": "@clawos/credentials",
  "version": "0.0.1",
  "private": true,
  "description": "ClawU — on-chain agent credential management for ClawOS.",
  "scripts": {
    "build": "forge build",
    "test": "forge test"
  }
}
